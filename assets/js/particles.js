// assets/js/particles.js — NEXOS Subtle Background Effects (CSS-only, no canvas)
// This module is intentionally lightweight — background effects are handled via CSS
// This file provides JS-enhanced interactive effects only when needed
(function() {
    'use strict';
    // No heavy canvas particles. All background effects use CSS pseudo-elements
    // defined in animations.css (.nx-bg-grid, .nx-bg-dots, .nx-glow-orb)
    // This keeps the site performant while maintaining visual quality
})();
